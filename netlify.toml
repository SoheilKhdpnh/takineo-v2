[build]
  command = "npm run build"
  publish = ".next"

[context.production]
  command = "npm run db:migrate:deploy && npm run build"

[functions."mux-playback-reconciliation"]
  schedule = "* * * * *"
