import {
  TeacherWorkspacePage,
  type TeacherWorkspacePageProps,
} from "@/app/[locale]/teacher/teacher-workspace-page";

export default async function TeacherDashboardPage(
  props: TeacherWorkspacePageProps,
) {
  return TeacherWorkspacePage(props);
}
